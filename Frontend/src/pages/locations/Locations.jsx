import { Container } from "react-bootstrap";

export default function Locations() {

    return (
        <Container>
            Here will be displayed all existing locations
        </Container>
    );
}