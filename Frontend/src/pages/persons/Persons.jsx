import { Container } from "react-bootstrap";

export default function Persons() {

    return (
        <Container>
            Here will be displayed all existing persons
        </Container>
    );
}